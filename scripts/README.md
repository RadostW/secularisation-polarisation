The scripts folder is organised as follows. To run everything in correct order simply run

```bash
python3 main.py
```

To produce intermediate, clean, harmonised files run

```bash
python3 combine.py
```

To produce final results from the intermediate files run

```bash
python3 model.py
```

Other files in this directory can depend on running in correct order.